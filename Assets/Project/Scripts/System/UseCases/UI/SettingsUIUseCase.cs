using System;
using VContainer.Unity;

namespace Project.Scripts.UI.UseCases
{
    // Reserved use case for future Settings logic (music/sound persistence, runtime apply, etc.)
    public class SettingsUIUseCase : IInitializable, IDisposable
    {
        public void Initialize()
        {
        }

        public void Dispose()
        {
        }
    }
}
